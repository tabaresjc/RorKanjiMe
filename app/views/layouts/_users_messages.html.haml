- if @user.errors.any?
  %div{:class=>"alert alert-danger", :style=>"text-align: left; margin:0px 15px;"}
    = "The form contains #{pluralize(@user.errors.count, "error")}"
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= "#{msg}"
    